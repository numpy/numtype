version: 2

updates:
- package-ecosystem: github-actions
  directory: /
  schedule:
    interval: weekly
  labels:
  - "topic: dependencies"
  - "tool: github-actions"
  commit-message:
    prefix: "⬆️ "
  groups:
    actions:
      patterns: ["*"]

# Renovate takes care of uv
